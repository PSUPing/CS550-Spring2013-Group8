class test(num)
L := num;
noc := proc(x) return := cons(x,L) end;
rac :=proc() return := car(L) end;
rdc :=proc() return := cdr(L) end
end;

L:=test([]);
A:=L.noc(3);
B:=L.noc(2);
C:=L.noc(1);
x:=L.rac();
M:=L.rdc()
